/* Insert a notice between the last comment and the comment form */
$("#contact-list-container").html("<%= escape_javascript(render(:partial => 'contacts/contacts_icons', :locals => {:contacts => @contacts})) %>");
